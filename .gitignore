# Dependencies
/node_modules

# Build results
/dist

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store

> **파일 설명**:
> * `/node_modules`: 가장 중요한 부분으로, `node_modules` 폴더 전체를 무시하라는 뜻입니다.
> * `/dist`: `npm run build` 실행 시 생성되는 빌드 결과물 폴더도 보통 올리지 않으므로 추가했습니다.

#### **2단계: Git의 추적 기록 초기화 및 재설정**

이미 `git add .` 명령어로 `node_modules`를 "업로드할 파일 목록"에 추가해버렸기 때문에, 이 기록을 깨끗하게 지우고 다시 설정해야 합니다.

프로젝트 폴더의 터미널에서 아래 명령어들을 **순서대로** 실행하세요.

1.  **"업로드할 파일 목록"에서 모든 파일의 추적을 임시로 해제합니다.**
    ```bash
    git rm -r --cached .
    ```

2.  **이제 `.gitignore`의 규칙을 적용하여 다시 모든 파일을 추가합니다.**
    ```bash
    git add .
    ```
    (이제 Git은 `.gitignore`에 적힌 `node_modules` 폴더를 무시하고 나머지 파일만 추가합니다.)

3.  **변경 사항을 새로운 메시지로 저장(commit)합니다.**
    ```bash
    git commit -m "Add .gitignore to exclude node_modules"
    ```

#### **3단계: GitHub에 다시 푸시(Push)하기**

이제 모든 준비가 끝났습니다. `node_modules` 폴더가 제외되었기 때문에 용량 문제 없이 업로드될 것입니다. 아래 명령어로 다시 푸시하세요.

```bash
git push origin main